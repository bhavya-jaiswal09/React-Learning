import React from 'react'

const Hero = ({darkMode}) => {
  return (
    <div>
        <div style={{backgroundColor:(darkMode ? "blue" : "white"), height:"80vh", width:"100vw"}}>

        </div>
    </div>
  )
}

export default Hero